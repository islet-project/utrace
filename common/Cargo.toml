[package]
name = "utrace_common"
version.workspace = true
authors.workspace = true
edition.workspace = true

build = "../build.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
